/*
 * Exercise 04 — Logical Operators
 *
 * Check if a year is a leap year.
 * A year is a leap year if it's divisible by 400, or divisible by 4 but not 100.
 *
 * Expected output (with year = 2024):
 *   2024 is a leap year.
 *
 * Hints:
 *   - Use && (AND), || (OR), ! (NOT)
 *   - year % 400 == 0 || (year % 4 == 0 && year % 100 != 0)
 */
