/*
 * Exercise 06 — Environment Variables
 *
 * Read and print the value of an environment variable using getenv.
 *
 * Expected output (value depends on your system):
 *   HOME = /home/nisidabay
 *
 * Hints:
 *   - char *home = getenv("HOME");
 *   - if (home == NULL) { printf("HOME not set\n"); }
 *   - else { printf("HOME = %s\n", home); }
 *   - #include <stdlib.h>, <stdio.h>
 */
