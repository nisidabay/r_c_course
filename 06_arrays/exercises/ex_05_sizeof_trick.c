/*
 * Exercise 05 — Sizeof Trick
 *
 * Print the number of elements in an array using sizeof.
 *
 * Expected output:
 *   Array size: 8 elements
 *
 * Hints:
 *   - int arr[] = {5, 10, 15, 20, 25, 30, 35, 40};
 *   - int count = sizeof(arr) / sizeof(arr[0]);
 *   - sizeof(arr) gives total bytes, divide by element size
 */
