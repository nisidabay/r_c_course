/*
 * Exercise 02 — Text I/O
 *
 * Write a program that reads from a text file and prints it line by line.
 *
 * Expected output:
 *   Line 1: Hello
 *   Line 2: World
 *   Line 3: from C
 *
 * Hints:
 *   - First create a file with 3 lines using fprintf
 *   - Reopen for reading, then while (fgets(buf, sizeof(buf), fp) != NULL)
 *   - Print each line with a line number
 *   - fclose and remove at the end
 */
