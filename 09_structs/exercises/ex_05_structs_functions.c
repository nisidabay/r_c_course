/*
 * Exercise 05 — Structs and Functions
 *
 * Pass a struct to a function and return a struct from a function.
 *
 * Expected output:
 *   Point (3, 4) distance from origin: 5.00
 *
 * Hints:
 *   - double distance(Point p) { return sqrt(p.x*p.x + p.y*p.y); }
 *   - Point translate(Point p, int dx, int dy) { p.x += dx; p.y += dy; return p; }
 *   - #include <math.h> and compile with -lm
 */
