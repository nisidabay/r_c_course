/*
 * Exercise 03 — Pass by Value
 *
 * Write a function that tries to double a value — show that the caller
 * is NOT affected because C passes by value.
 *
 * Expected output:
 *   Before: x = 5
 *   Inside double_it: y = 10
 *   After: x = 5
 *
 * Hints:
 *   - void double_it(int y) — modifies y, but that's a copy
 *   - Show x stays 5 after the call
 */
