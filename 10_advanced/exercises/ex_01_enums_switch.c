/*
 * Exercise 01 — Enums and Switch
 *
 * Define an enum for the days of the week and print the name for a given number.
 *
 * Expected output:
 *   3 is Wednesday
 *
 * Hints:
 *   - enum Day { MON=1, TUE, WED, THU, FRI, SAT, SUN };
 *   - enum Day d = WED;
 *   - switch(d) { case MON: printf("Monday"); break; ... }
 *   - #include <stdio.h>, <stdlib.h>
 */
