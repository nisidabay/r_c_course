/*
 * Exercise 03 — Null Pointer
 *
 * Demonstrate a null pointer and why you must check before dereferencing.
 *
 * Expected output:
 *   ptr is NULL — cannot dereference safely
 *
 * Hints:
 *   - int *ptr = NULL;
 *   - if (ptr == NULL) { printf("ptr is NULL\n"); }
 *   - NEVER do *ptr when it's NULL — that's a segfault
 */
