/*
 * Exercise 04 — Pointers as Parameters
 *
 * Write a function that swaps two integers using pointers.
 *
 * Expected output:
 *   Before: a = 5, b = 10
 *   After:  a = 10, b = 5
 *
 * Hints:
 *   - void swap(int *a, int *b) { int temp = *a; *a = *b; *b = temp; }
 *   - Call it: swap(&x, &y);
 */
