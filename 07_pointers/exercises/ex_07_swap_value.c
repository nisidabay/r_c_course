/*
 * Exercise 07 — Swap by Value (Fallar para Aprender)
 *
 * Completa la función swap_by_value que INTENTA intercambiar
 * dos enteros USANDO SOLO PARÁMETROS POR VALOR.
 *
 * Observa que, aunque la función intercambia sus copias locales,
 * los valores originales en main() NO cambian.
 *
 * Expected output:
 *   Before: a = 5, b = 10
 *   (inside swap_by_value: x = 10, y = 5)
 *   After:  a = 5, b = 10
 *
 * Hints:
 *   - C pasa TODO por valor — swap_by_value recibe COPIAS
 *   - El intercambio dentro de la función solo cambia las copias
 *   - Los valores originales a y b NO se modifican
 */
