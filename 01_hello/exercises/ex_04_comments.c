#include <stdio.h>

int main(void)
{
    /*
     * Task: Add comments to describe what this program does.
     *
     * 1. Add a single-line comment at the top saying "Age calculator program"
     * 2. Add a multi-line comment before the printf explaining each step
     * 3. Add an inline comment on the same line as total = x + y
     *
     * Replace each placeholder with an appropriate comment.
     */

    // --- your code here (single-line comment) ---

    /* --- your code here (multi-line comment) --- */

    int age = 15;
    int years = 7;

    int future_age = age + years;  // --- your code here (inline comment) ---

    printf("In %d years you will be %d.\n", years, future_age);

    return 0;
}
