#include <stdio.h>

int main(void)
{
    int a = 20;
    int b = 6;

    /*
     * Task: Compute the following using a and b:
     *   - sum (a + b)
     *   - difference (a - b)
     *   - product (a * b)
     *   - quotient (a / b)       -- integer division
     *   - remainder (a % b)      -- modulus
     *
     * Uncomment and complete each line with the correct expression.
     * Replace YOUR_CODE_HERE with the arithmetic expression.
     */

    int sum = 0;
    // sum = YOUR_CODE_HERE;

    int diff = 0;
    // diff = YOUR_CODE_HERE;

    int prod = 0;
    // prod = YOUR_CODE_HERE;

    int quot = 0;
    // quot = YOUR_CODE_HERE;

    int rem = 0;
    // rem = YOUR_CODE_HERE;

    printf("a = %d, b = %d\n", a, b);
    printf("a + b = %d\n", sum);
    printf("a - b = %d\n", diff);
    printf("a * b = %d\n", prod);
    printf("a / b = %d\n", quot);
    printf("a %% b = %d\n", rem);

    return 0;
}
